apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.name }}-database-pvc
spec:
  storageClassName: gp2
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 3Gi